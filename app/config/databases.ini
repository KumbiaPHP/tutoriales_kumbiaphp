; Kumbia Web Framework Configuration

; Parámetros de base de datos
; Utiliza el nombre del controlador nativo (mysql, pgsql, oracle)
; Coloca database.pdo = On si usas PHP Data Objects

[mi_conexion_a_tutoriales_mysql]
host = localhost
username = root
password = 
name = tutoriales_kumbiaphp
type = mysqli

[production]
host = localhost
username = root
password =
name = test
type = pgsql

;Ejemplo de SQLite

;[development]
;dsn = "test/data.db"
;type = sqlite
;pdo = On

;Ejemplo de PDO con MySQL

;[development]
;dsn = "host=localhost;dbname=test"
;username = root
;password =
;type = mysql
;pdo = On